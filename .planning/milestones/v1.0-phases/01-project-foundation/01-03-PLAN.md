---
phase: 01-project-foundation
plan: 03
type: execute
wave: 1
depends_on: ["01-02"]
files_modified: ["frontend/src/pages/Projects.jsx", "frontend/src/components/layout/Sidebar.jsx"]
autonomous: true
requirements: [PROJ-02, NAV-01]
gap_closure: true

must_haves:
  truths:
    - "Projects empty state shows a clean layout with proper spacing and a single clear create action"
    - "Sidebar projects section shows a visible status indicator dot next to each project name"
  artifacts:
    - path: "frontend/src/pages/Projects.jsx"
      provides: "Clean empty state with single CTA and proper spacing"
    - path: "frontend/src/components/layout/Sidebar.jsx"
      provides: "Visible status dots and error logging for project fetch"
  key_links:
    - from: "frontend/src/pages/Projects.jsx"
      to: "empty state UX"
      via: "single New Project button in header, empty state has guidance text only"
      pattern: "flex-col items-center justify-center"
    - from: "frontend/src/components/layout/Sidebar.jsx"
      to: "project status display"
      via: "w-2 h-2 status dot with color mapping"
      pattern: "w-2 h-2 rounded-full"
---

<objective>
Fix two UAT-reported UI issues from phase 01: (1) the projects empty state has a duplicate "New Project" button and zero vertical padding making it look cramped, and (2) the sidebar projects section has status dots too small to see and silently swallows fetch errors.

Purpose: Close the two remaining UAT gaps so phase 01 passes all acceptance criteria.
Output: Two patched frontend files resolving both visual issues.
</objective>

<execution_context>
@C:/Users/prann/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/prann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix projects empty state layout</name>
  <files>frontend/src/pages/Projects.jsx</files>
  <action>
In `frontend/src/pages/Projects.jsx`, fix the empty state block (currently around lines 337-353):

1. REMOVE the duplicate "New Project" button from the empty state block entirely (lines ~346-352 — the `<button onClick={() => setShowCreateModal(true)}` inside the empty state container). The header already has an identical "New Project" button (lines ~317-323) that is always visible. Having two identical buttons is confusing.

2. ADD vertical padding to the empty state container. Change the empty state wrapper div from:
   `className="flex-1 flex flex-col items-center justify-center gap-4 text-center px-6"`
   to:
   `className="flex-1 flex flex-col items-center justify-center gap-4 text-center px-6 pt-12"`

   This adds top padding so the flask icon and text don't sit flush against the header border.

3. CHANGE the gap between icon and text from `gap-4` to `gap-6` to give more breathing room.

The result should be: header has the "New Project" button, and below it the empty state shows just the flask icon, heading, and subtitle with comfortable spacing — no duplicate button.
  </action>
  <verify>
    <automated>cd C:/Users/prann/projects/researchos/frontend && npx grep-cli "New Project" src/pages/Projects.jsx | wc -l</automated>
    Verify "New Project" appears exactly once in the file (the header button only). Also visually confirm the empty state has proper vertical spacing.
  </verify>
  <done>Empty state shows flask icon + text only (no duplicate button), with comfortable vertical spacing from the header. "New Project" text appears exactly once in Projects.jsx.</done>
</task>

<task type="auto">
  <name>Task 2: Fix sidebar project status dots and error handling</name>
  <files>frontend/src/components/layout/Sidebar.jsx</files>
  <action>
In `frontend/src/components/layout/Sidebar.jsx`, fix the ProjectsTree component (starts around line 716):

1. INCREASE the status dot size from `w-1.5 h-1.5` to `w-2 h-2` (from 6px to 8px) on line ~790. Change:
   `className={`w-1.5 h-1.5 rounded-full flex-shrink-0 ${dotClass}`}`
   to:
   `className={`w-2 h-2 rounded-full flex-shrink-0 ${dotClass}`}`

2. REPLACE the silent `.catch(() => {})` on line ~727 with proper error logging:
   `.catch(err => console.error('ProjectsTree: failed to fetch projects:', err))`

   This ensures that if the API call fails, the developer can see why in the console instead of silently showing no projects.

Do NOT change the color mapping in PROJECT_STATUS_COLORS — those are correct. Only change the dot size and error handler.
  </action>
  <verify>
    <automated>cd C:/Users/prann/projects/researchos/frontend && grep -n "w-2 h-2 rounded-full flex-shrink-0" src/components/layout/Sidebar.jsx && grep -n "console.error.*ProjectsTree" src/components/layout/Sidebar.jsx</automated>
    Both grep commands should return matching lines confirming the dot size increase and error logging.
  </verify>
  <done>Sidebar project status dots are 8px (visibly larger), and fetch errors are logged to console instead of silently swallowed.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` completes without errors
2. Start the app, navigate to /projects with no projects — single "New Project" button in header, empty state has flask icon + text with comfortable spacing
3. Create a project, check sidebar — status dot is clearly visible next to project name
</verification>

<success_criteria>
- Projects empty state has exactly one "New Project" button (in the header) with proper vertical spacing
- Sidebar status dots are w-2 h-2 (8px) and visually prominent
- No silent error swallowing in ProjectsTree fetch
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-03-SUMMARY.md`
</output>
