---
phase: 09-task-database
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/pages/ProjectTasks.tasks.test.jsx
autonomous: true
requirements:
  - TASK-02
  - TASK-03
  - TASK-06

must_haves:
  truths:
    - "Test file exists with stub tests for pure helper functions"
    - "All stub tests fail (RED phase) since implementations do not exist yet"
  artifacts:
    - path: "frontend/src/pages/ProjectTasks.tasks.test.jsx"
      provides: "Stub tests for getMonthGrid, applyTaskFilter, sortTaskRows, isOverdue"
      min_lines: 30
  key_links:
    - from: "frontend/src/pages/ProjectTasks.tasks.test.jsx"
      to: "frontend/src/pages/ProjectTasks.jsx"
      via: "imports helper functions"
      pattern: "import.*ProjectTasks"
---

<objective>
Create a test scaffold with failing stub tests for the pure helper functions that Plans 02-04 will implement: `getMonthGrid`, `applyTaskFilter`, `sortTaskRows`, and `isOverdue`.

Purpose: Establishes the Nyquist verification baseline so that Plans 02-04 can use fast `npm run test:run` verification instead of slow `vite build` checks. Tests are RED initially and go GREEN as each plan implements its functions.
Output: A test file with 4+ failing test stubs.
</objective>

<execution_context>
@C:/Users/prann/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/prann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-task-database/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub test file for task view helper functions</name>
  <files>frontend/src/pages/ProjectTasks.tasks.test.jsx</files>
  <action>
Create `frontend/src/pages/ProjectTasks.tasks.test.jsx` with the following stub tests using vitest (`describe`/`it`/`expect`). Each test imports from `./ProjectTasks` and asserts expected behavior — they will fail until the corresponding plan implements the function.

```js
import { describe, it, expect } from 'vitest';
import { getMonthGrid, applyTaskFilter, sortTaskRows, isOverdue } from './ProjectTasks';
```

**getMonthGrid tests (Plan 04 — CalendarView):**
- `getMonthGrid(2026, 2)` (March 2026) returns a 42-element array
- First element is a Sunday (or null if month doesn't start on Sunday)
- Day 1 of March 2026 is at the correct index (March 1, 2026 is a Sunday → index 0)
- Day 31 is present, day 32 is not
- All non-null elements are Date objects

**applyTaskFilter tests (Plan 03 — ListView):**
- Returns all tasks when filters array is empty
- Filters by status (column_id match)
- Filters by priority (exact match on 'high', 'medium', 'low', 'none')
- Filters by overdue (due_date < today)
- Multiple filters are ANDed together

**sortTaskRows tests (Plan 03 — ListView):**
- Sorts by title alphabetically ascending
- Sorts by priority by rank (high=0, medium=1, low=2, none=3)
- Sorts by due_date chronologically (nulls last)
- Sorts descending when direction is 'desc'

**isOverdue tests (Plans 02/04 — Kanban/Calendar):**
- Returns true when dueDate is before today (YYYY-MM-DD string comparison)
- Returns false when dueDate is today or future
- Returns false when dueDate is null or undefined

Note: These tests WILL FAIL because ProjectTasks.jsx does not yet export these functions. That is intentional — this is the RED phase. Plans 02-04 must export these functions from ProjectTasks.jsx.
  </action>
  <verify>
    <automated>cd frontend && npm run test:run -- ProjectTasks.tasks.test 2>&1 | tail -10 || echo "Tests failed as expected (RED phase)"</automated>
  </verify>
  <done>
    - Test file exists at frontend/src/pages/ProjectTasks.tasks.test.jsx
    - Contains stub tests for getMonthGrid, applyTaskFilter, sortTaskRows, isOverdue
    - Tests fail because implementations don't exist yet (RED phase confirmed)
  </done>
</task>

</tasks>

<verification>
- Test file imports from ProjectTasks and has at least 12 test cases across 4 describe blocks
- Running `npm run test:run -- ProjectTasks.tasks.test` shows failing tests (not import errors from missing vitest)
</verification>

<success_criteria>
- Test scaffold created with all 4 function test suites
- Tests are in RED state (failing) — confirms they will validate implementation later
</success_criteria>

<output>
After completion, create `.planning/phases/09-task-database/09-00-SUMMARY.md`
</output>
